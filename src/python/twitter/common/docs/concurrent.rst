`concurrent`
============

.. py:module:: twitter.common.concurrent

Includes a 2.6.x backport of the 3.x `concurrent.futures`
library. Also includes `deadline(...)`, `defer(...)` and a threaded
event multiplexer.
